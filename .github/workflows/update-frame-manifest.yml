name: Update frame manifest

on:
  push:
    branches: [ main ]
    paths:
      - "frames/**"
      - "scripts/build_frame_manifest.py"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-manifest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build frame manifest
        run: python scripts/build_frame_manifest.py

      - name: Commit manifest if changed
        run: |
          if git diff --quiet frames/manifest.json; then
            echo "Manifest already up to date."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add frames/manifest.json
          git commit -m "Update frame manifest [skip ci]"
          git push

